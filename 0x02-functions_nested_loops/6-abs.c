#include "main .h"
#include <stdlib.h>
/**
 * _abs - Computes the absolute value of an integer.
 *
 * Return: Always 0
 */
int _abs(int)
{
int x;
if (x == 0 || x > 0)
{
return (x);
}
else
{
return (-1 * x);
}
}
