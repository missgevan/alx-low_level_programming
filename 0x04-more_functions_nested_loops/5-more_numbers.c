#include "main.h"

/**
 * more_numbers -  prints 10 times numbers 0 to 14
 *
 * Return: void
 */

void more_numbers(void)
{
	int a, b;

	for (b = 0 ; b <= 9 ; b++)
	{
		for (a = 0 ; a <= 14 ; a++)
		{
			_putchar(a);
		}
			_putchar('\n');
			}
		}
